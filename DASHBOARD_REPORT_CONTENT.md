# เนื้อหาหน้า "รายงานแบบแดชบอร์ด" — สำหรับนำไปออกแบบใน Figma

**หน้า:** `/admin/reports/print/dashboard` (ไฟล์: `backend-php/src/reports/dashboard.php`)
**ผู้ใช้:** แอดมิน/เจ้าหน้าที่ห้องสมุด
**บริบท:** เป็น 1 ใน 8 ประเภทรายงานของระบบ (รายวัน, รายเดือน, รายแผนก, สรุปผู้บริหาร, เปรียบเทียบ, รายภาคเรียน, ค้นหานักศึกษา, และ**แดชบอร์ด**) — เข้าถึงผ่านหน้าเลือกประเภทรายงาน (`/admin/reports/print`, คนละไฟล์คนละหน้าโดยสมบูรณ์) แล้วพิมพ์/บันทึกเป็น PDF ได้

> **สถานะ:** เอกสารนี้อธิบายสภาพ**ที่ทำเสร็จแล้วจริง** (as-built) ไม่ใช่ข้อเสนอ — ใช้อ้างอิงตอนคุยกับ Figma ได้ตรงกับของจริงในเว็บ

---

## 1. โครงสร้างหน้า

1 หน้า แบ่งเป็น **3 แท็บ** ใต้แถบตัวกรองที่คงที่ (ไม่ต้องกรอกซ้ำเวลาสลับแท็บ): **ภาพรวม** (ค่าเริ่มต้นเมื่อเปิดหน้า) / **แนวโน้ม** / **แผนกวิชา** ปุ่มพิมพ์พิมพ์เฉพาะแท็บที่เปิดอยู่ (แท็บที่ไม่ได้เปิดใช้ `hidden` attribute ซึ่งเบราว์เซอร์ข้ามตอนพิมพ์อยู่แล้วโดยธรรมชาติ)

## 2. แถบตัวกรอง (Filter bar) — คงที่บนสุด ใช้ร่วมกันทุกแท็บ

- **Quick filter chips:** "สัปดาห์นี้" (จันทร์–วันนี้ของสัปดาห์ปัจจุบัน) / "เดือนนี้" — คลิกแล้วตั้งค่าฟอร์มด้านล่างให้อัตโนมัติ
- เดือน (month picker) / จากวันที่–ถึงวันที่ (ช่วงกำหนดเอง แทนเดือนได้) / ปีการศึกษา / ภาคเรียน / แผนกวิชา / ระดับชั้น (dropdown ทั้งหมด) / ปุ่ม "กรองข้อมูล" / "ล้างตัวกรอง"

## 3. แท็บ "ภาพรวม"

- **Hero tile:** จำนวน**นักศึกษาที่เข้าใช้บริการไม่ซ้ำคน**ในช่วงเวลาที่เลือก + % เทียบช่วงก่อนหน้า (▲/▼ เฉพาะตอนกรองเป็นเดือน — ช่วงวันที่กำหนดเองไม่มี baseline เทียบ)
- **Stat tiles (5 ใบ)** แต่ละใบมี sparkline ฝังมุมขวาบนของการ์ดเอง (ยกเว้นใบเวลาเฉลี่ย ซึ่งยังไม่มีข้อมูลอนุกรมรายวันให้ทำกราฟ):
  1. จำนวนรายการทั้งหมด (check-in+check-out) + % เทียบช่วงก่อนหน้า
  2. เฉลี่ยต่อวัน (เฉพาะวันที่มีข้อมูล)
  3. **เวลาเฉลี่ยที่อยู่ในห้องสมุดต่อครั้ง** — จับคู่เวลาเช็คอิน/เช็คเอาต์จริงต่อคน แสดงเป็น "X ชม. Y นาที"
  4. วันที่มีคนใช้มากที่สุด (busiest day)
  5. ช่วงเวลาที่มีคนใช้มากที่สุด (peak hour)
- **Top Active Department (meter tile):** วงแหวน % ส่วนแบ่งของแผนกอันดับ 1 + ชื่อแผนก + จำนวนรายการที่แผนกนั้นทำได้จริง (ไม่ใช่แค่ %)
- **Story box:** ประโยคสรุปภาษาคนอัตโนมัติ 1 ย่อหน้า (คำนวณจากค่าเดียวกับ tile ด้านบน ไม่มีทางเลขไม่ตรงกัน)

## 4. แท็บ "แนวโน้ม"

- กราฟแท่งแนวโน้มการเช็คชื่อรายวัน ตลอดช่วงเวลาที่เลือก
- กราฟแท่งการใช้งานแยกตามชั่วโมง (00:00–23:00)
- กราฟแท่งแนวโน้มรายสัปดาห์ในช่วงเวลาที่เลือก

## 5. แท็บ "แผนกวิชา"

รายชื่อ**แผนกวิชาทั้งหมด**ที่มีข้อมูล (ไม่ตัดจำนวน) เรียงจากใช้งานมากไปน้อย แต่ละแถว: เลขอันดับวงกลม (อันดับ 1 พื้นสีน้ำเงินเข้ม ที่เหลือพื้นฟ้าอ่อน) + ชื่อแผนก + แถบสัดส่วนสีเดียวไล่โทน + จำนวนรายการและ % ของยอดรวม

## 6. "ปรับแต่งวิดเจ็ต / ตั้งค่าการพิมพ์" (drawer)

Drawer เดียวกับปุ่ม "ตั้งค่าการพิมพ์" ที่รายงานทุกแบบมีอยู่แล้ว — สำหรับหน้านี้ปรับ label เป็น "ปรับแต่งวิดเจ็ต / ตั้งค่าการพิมพ์" มี: เลือกขนาดกระดาษ, ธีมสี, และ**checkbox เลือกว่าจะรวม widget ไหนตอนพิมพ์** (auto-generate จาก widget ที่มีอยู่จริงในแท็บที่กำลังเปิด — สลับแท็บแล้วรายการ checkbox จะอัปเดตตาม)

## 7. Export / Print

ปุ่ม "พิมพ์ / บันทึกเป็น PDF" ในแถบเครื่องมือด้านบน (เรียก `window.print()` แล้วเลือก "Save as PDF" ในเบราว์เซอร์ได้) — ใช้ปุ่มเดียวครอบคลุมทั้ง export PDF และ "พิมพ์รายงานสำหรับผู้บริหาร" ไม่มีปุ่มแยก

## 8. สถานะไม่มีข้อมูล (Empty state)

ข้อความ + ปุ่ม "เปลี่ยนช่วงเวลา" เมื่อไม่มีข้อมูลการเช็คชื่อในช่วงที่กรอง (แทนที่ทั้งหน้า ไม่แสดงแท็บ)

---

## 9. Widget ที่ตัดออก: Gender Breakdown

มีการขอ widget แสดงสัดส่วนเพศ (ชาย/หญิง) แต่**ตัดออกจากดีไซน์** เพราะฐานข้อมูลจริง (ตาราง `students`/`users`) **ไม่มีฟิลด์เพศเก็บอยู่เลย** และฟอร์มสมัครสมาชิก/สคริปต์นำเข้าข้อมูลก็ไม่ได้เก็บข้อมูลนี้ — ตัดสินใจไม่สร้างตัวเลขปลอมแสดงในรายงานที่ผู้บริหารจะเห็น ถ้าต้องการ widget นี้จริงๆ ต้องทำเป็นงานแยก: เพิ่มฟิลด์ใน schema + ฟอร์มสมัคร + สคริปต์นำเข้า + backfill ข้อมูลนักศึกษาที่มีอยู่ทั้งหมด

## 10. สิ่งที่ไม่เปลี่ยน / นอกขอบเขต

- การคำนวณตัวเลขอ้างอิงชุดฟังก์ชัน aggregate เดียวกับรายงานอื่น (`aggregate.php`) ยกเว้นฟังก์ชันใหม่ 2 ตัวที่ทำเฉพาะหน้านี้ (`aggregate_avg_session_minutes`, `format_minutes_thai` — อยู่ใน `dashboard.php` เอง ไม่กระทบรายงานอื่น)
- ไม่เกี่ยวกับระบบยืม-คืนหนังสือ (ไม่มีฟีเจอร์นี้ในระบบ)
- ไม่ใช่หน้า "ภาพรวม" แบบ interactive ของแอดมิน (`/admin/dashboard` ในเว็บแอป) — เอกสารนี้พูดถึงเฉพาะ**หน้ารายงานสำหรับพิมพ์** (`/admin/reports/print/dashboard`) เท่านั้น

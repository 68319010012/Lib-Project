# รายละเอียดของโปรเจคแบบละเอียด
## ระบบเช็คชื่อเข้าใช้ห้องสมุด — วิทยาลัยเทคนิคนครนายก

> เอกสารนี้สร้างจากการอ่านซอร์สโค้ดจริงทีละไฟล์ ไม่ได้เขียนจากความจำหรือจากเอกสารเก่า
> ตัวเลขบรรทัด ชื่อฟังก์ชัน และชนิดข้อมูลทั้งหมดตรงกับโค้ด ณ commit `fd4f2c4`
> วันที่จัดทำ: 22 สิงหาคม 2569

---

## สารบัญ

1. [ภาพรวมระบบ](#1-ภาพรวมระบบ)
2. [เทคโนโลยีที่ใช้](#2-เทคโนโลยีที่ใช้)
3. [โครงสร้างไฟล์](#3-โครงสร้างไฟล์)
4. [ฐานข้อมูล](#4-ฐานข้อมูล)
5. [ระบบยืนยันตัวตนและสิทธิ์](#5-ระบบยืนยันตัวตนและสิทธิ์)
6. [หน้าเว็บทั้งหมด](#6-หน้าเว็บทั้งหมด)
7. [ระบบรายงาน](#7-ระบบรายงาน)
8. [API ทั้งหมด 30 เส้นทาง](#8-api-ทั้งหมด-30-เส้นทาง)
9. [กลไกเบื้องหลัง](#9-กลไกเบื้องหลัง)
10. [ค่าคงที่และการตั้งค่า](#10-ค่าคงที่และการตั้งค่า)
11. [รายการฟังก์ชันทั้งหมด 231 ตัว](#11-รายการฟังก์ชันทั้งหมด-231-ตัว)
12. [การทดสอบ](#12-การทดสอบ)
13. [การนำขึ้นเซิร์ฟเวอร์](#13-การนำขึ้นเซิร์ฟเวอร์)

---

## 1. ภาพรวมระบบ

ระบบบันทึกการเข้า-ออกห้องสมุดของนักศึกษา แทนการเซ็นชื่อบนกระดาษ นักศึกษาสมัครบัญชีด้วยรหัสนักศึกษาของตัวเอง กดปุ่มเดียวเพื่อเช็คอินตอนเข้า และเช็คเอาต์ตอนออก เจ้าหน้าที่ห้องสมุดดูภาพรวมการใช้งาน อนุมัติบัญชี และออกรายงานเป็น PDF ได้

### แนวคิดหลักที่ต้องเข้าใจก่อนอ่านส่วนอื่น

**การมาใช้ห้องสมุด 1 ครั้ง = 2 แถวในฐานข้อมูล**
ตาราง `checkin_logs` เก็บ *เหตุการณ์* ไม่ได้เก็บ *การมา* หนึ่งครั้งที่มาใช้ห้องสมุดจะกลายเป็นแถว `type='in'` หนึ่งแถว และแถว `type='out'` อีกหนึ่งแถว ระบบจับคู่สองแถวนี้ด้วย `log_id` ที่อยู่ติดกัน แถว `in` ที่ไม่มี `out` ตามหลัง = คนนั้นยังอยู่ในห้องสมุด

**ปุ่มเช็คอินเป็นปุ่มสลับ (toggle)**
ไม่มีปุ่ม "เช็คอิน" กับ "เช็คเอาต์" แยกกัน มีปุ่มเดียว ระบบดูจากแถวล่าสุดของคนนั้นว่าตอนนี้อยู่ข้างในหรือข้างนอก แล้วสลับให้เอง

**ระบบปิดการเข้าใช้ให้เองเมื่อถึงเวลา**
นักศึกษาที่ลืมกดเช็คเอาต์จะถูกระบบปิดให้อัตโนมัติ ไม่ต้องมีคนไปตามกด และไม่ต้องมี cron job แยก (ดูหัวข้อ 9)

**ไม่มีอีเมลในระบบ**
ฐานข้อมูลไม่เก็บอีเมลหรือเบอร์โทรของใครเลย จึงทำ "ลืมรหัสผ่าน" แบบส่งลิงก์ทางอีเมลไม่ได้ ต้องให้เจ้าหน้าที่รีเซ็ตให้แล้วบอกรหัสชั่วคราวกับนักศึกษาต่อหน้า

---

## 2. เทคโนโลยีที่ใช้

| ส่วน | เทคโนโลยี | หมายเหตุ |
|---|---|---|
| ภาษาฝั่งเซิร์ฟเวอร์ | PHP 8.x | ไม่ใช้ framework ใดๆ เขียน front controller เอง |
| ฐานข้อมูล | MariaDB 10.4 / MySQL 8 | ใช้ window function `LEAD()` ซึ่งรองรับตั้งแต่ MariaDB 10.2 |
| การเชื่อมต่อ DB | PDO | ปิด emulated prepares ใช้ prepared statement จริง |
| ฝั่งหน้าเว็บ | HTML + JavaScript ล้วน | ไม่มี React/Vue ไม่มี build step |
| CSS | Tailwind ที่ build ไว้ล่วงหน้า | ไฟล์เดียว `styles.css` ไม่มีขั้นตอน compile ตอน deploy |
| ฟอนต์ | IBM Plex Sans Thai + IBM Plex Mono | โฮสต์เอง ไม่เรียก Google Fonts จึงใช้งานออฟไลน์ได้ |
| สร้าง PDF | mPDF | ไลบรารีเดียวที่ใช้ ติดตั้งผ่าน Composer |
| สร้างกราฟใน PDF | GD (ฟังก์ชันในตัวของ PHP) | วาดกราฟเป็นรูป PNG แล้วให้ mPDF วาง |

### สิ่งที่ระบบ **ไม่** ใช้ และเหตุผล

- **ไม่มี framework** — ทุก endpoint เป็น path ตายตัว ไม่มี dynamic segment แผนที่ `"METHOD path" => ฟังก์ชัน` แบบแบนๆ จึงพอแล้ว ไม่ต้องมี router ที่ใช้ regex
- **ไม่มี background scheduler / cron** — งานเบื้องหลังรันแทรกในทุก request แทน (ดูหัวข้อ 9)
- **ไม่มี Composer dependency นอกจาก mPDF** — การส่งออก CSV ใช้ฟังก์ชันในตัวของ PHP และ "Excel" คือตาราง HTML ที่ตั้งนามสกุล `.xls`
- **ไม่มี CORS** — หน้าเว็บกับ API เสิร์ฟจาก origin เดียวกัน เบราว์เซอร์จึงไม่เคยยิงข้ามโดเมน

---

## 3. โครงสร้างไฟล์

```
backend-php/
├── public/                     ← document root (public_html บนเซิร์ฟเวอร์จริง)
│   ├── index.php               ← front controller — ทุก API ผ่านที่นี่
│   ├── login.php               ← หน้าเข้าสู่ระบบ
│   ├── signup.php              ← หน้าสมัครสมาชิก
│   ├── dashboard.php           ← หน้าหลักนักศึกษา (ปุ่มเช็คอิน)
│   ├── profile.php             ← หน้าโปรไฟล์นักศึกษา
│   ├── admin-dashboard.php     ← ภาพรวมสำหรับเจ้าหน้าที่
│   ├── admin-members.php       ← ทำเนียบสมาชิก
│   ├── admin-active.php        ← ใครอยู่ในห้องสมุดตอนนี้
│   ├── admin-logs.php          ← ประวัติการเช็คชื่อทั้งหมด
│   ├── partials/               ← ชิ้นส่วน HTML ที่ใช้ซ้ำ
│   │   ├── guard.php           ← ยามเฝ้าประตู เช็คสิทธิ์ก่อนแสดงหน้า
│   │   ├── head.php            ← <head> ร่วม + ตัวกัน cache
│   │   ├── header.php          ← แถบบนสุด (โลโก้ ธีม เมนูบัญชี)
│   │   ├── admin-sidebar.php   ← เมนูข้างของเจ้าหน้าที่
│   │   ├── student-sidebar.php ← เมนูข้างของนักศึกษา
│   │   ├── confirm-modal.php   ← กล่องยืนยันร่วม
│   │   ├── history-modal.php   ← หน้าต่างประวัติการเข้าใช้
│   │   └── lib-banner.php      ← แบนเนอร์หัวหน้าล็อกอิน
│   └── assets/
│       ├── css/styles.css      ← CSS ทั้งระบบ ไฟล์เดียว
│       ├── css/report-fonts.css← @font-face สำหรับหน้ารายงาน
│       ├── js/                 ← สคริปต์แยกตามหน้า
│       ├── fonts/              ← ฟอนต์ .woff2 (โฮสต์เอง)
│       └── img/
├── src/                        ← โค้ดที่เข้าจากเว็บโดยตรงไม่ได้
│   ├── env.php                 ← อ่านไฟล์ .env
│   ├── db.php                  ← เชื่อมต่อฐานข้อมูล
│   ├── auth.php                ← session + เช็คสิทธิ์
│   ├── constants.php           ← ค่าคงที่ + ตรวจความถูกต้องของข้อมูล
│   ├── helpers.php             ← ฟังก์ชันช่วยทั่วไป + เวลา/วันเปิดทำการ
│   ├── rate_limit.php          ← กันเดารหัสผ่าน
│   ├── handlers/               ← ตัวจัดการ API แยกตามหมวด (auth, checkin,
│   │                              profile, admin, announcement)
│   └── reports/                ← ระบบรายงาน 8 แบบ + ตัวช่วยร่วม
├── fonts/                      ← ฟอนต์ .ttf สำหรับ mPDF (คนละไฟล์กับของเว็บ)
├── migrations/                 ← สคริปต์แก้ไขฐานข้อมูล
├── tests/smoke_test.php        ← ชุดทดสอบ end-to-end
├── schema.sql                  ← โครงสร้างฐานข้อมูล
└── .env                        ← รหัสฐานข้อมูล (ไม่อยู่ใน git)
```

### เหตุผลที่แยก `public/` กับ `src/`

เฉพาะโฟลเดอร์ `public/` เท่านั้นที่เว็บเข้าถึงได้ ไฟล์ใน `src/` อยู่นอก document root คนภายนอกจึงเปิดดูโค้ดไม่ได้ ระบบ deploy มีการตรวจข้อนี้ทุกครั้ง — เรียก `/src/auth.php` แล้วต้องได้ 404 ถ้าได้อย่างอื่นแปลว่าอัปไฟล์ผิดชั้นและจะหยุดทันที

---

## 4. ฐานข้อมูล

ฐานข้อมูลชื่อ `library_checkin` มี **5 ตาราง** ทุกตารางใช้ `utf8mb4` / `utf8mb4_unicode_ci` เพื่อรองรับภาษาไทยและอิโมจิได้ครบ

### 4.1 ตาราง `students` — ข้อมูลนักศึกษา (11 คอลัมน์)

| คอลัมน์ | ชนิด | ข้อกำหนด | ความหมาย |
|---|---|---|---|
| `student_id` | VARCHAR(20) | **PRIMARY KEY** | รหัสนักศึกษา ใช้เป็น username ด้วย |
| `prefix` | VARCHAR(20) | NOT NULL | คำนำหน้าชื่อ (นาย / นาง / นางสาว) |
| `gender` | ENUM | `male`,`female` เว้นว่างได้ | เพศ |
| `first_name` | VARCHAR(100) | NOT NULL | ชื่อ |
| `last_name` | VARCHAR(100) | NOT NULL | นามสกุล |
| `department` | VARCHAR(100) | | แผนกวิชา |
| `level` | VARCHAR(10) | | ระดับชั้น (ปวช. / ปวส.) |
| `year_level` | VARCHAR(20) | | ชั้นปีที่ |
| `room` | VARCHAR(10) | | ห้องเรียน |
| `semester` | VARCHAR(10) | | ภาคเรียน |
| `academic_year` | VARCHAR(10) | | ปีการศึกษา |

**ทำไม `gender` ถึงเว้นว่างได้:** สคริปต์นำเข้ารายชื่อนักศึกษาไม่ได้เก็บเพศมาด้วย มีแต่ตอนนักศึกษาสมัครเองที่กรอก บัญชีที่นำเข้ามาจึงเป็นค่าว่าง ระบบมีฟังก์ชัน `gender_from_prefix()` ไว้เดาจากคำนำหน้าชื่อเป็นค่าสำรอง

### 4.2 ตาราง `users` — บัญชีเข้าสู่ระบบ (8 คอลัมน์)

| คอลัมน์ | ชนิด | ข้อกำหนด | ความหมาย |
|---|---|---|---|
| `user_id` | INT | **PK**, AUTO_INCREMENT | รหัสหลัก |
| `username` | VARCHAR(50) | NOT NULL, **UNIQUE** | ชื่อผู้ใช้ (นักศึกษาใช้รหัสนักศึกษา) |
| `password_hash` | VARCHAR(255) | NOT NULL | รหัสผ่านที่ผ่าน bcrypt แล้ว |
| `role` | ENUM | `student`,`admin` NOT NULL | บทบาท |
| `student_id` | VARCHAR(20) | **FK** → `students`, ว่างได้ | บัญชีแอดมินไม่ผูกกับนักศึกษาคนใด |
| `id_card_photo_path` | VARCHAR(255) | | ที่อยู่ไฟล์รูปบัตรนักศึกษาที่อัปตอนสมัคร |
| `account_status` | ENUM | `pending`,`approved`,`retired` ค่าเริ่มต้น `pending` | สถานะบัญชี |
| `created_at` | DATETIME | NOT NULL ค่าเริ่มต้น NOW() | วันที่สมัคร ใช้เป็นจุดนับอายุบัญชี |

**สถานะบัญชี 3 แบบ**
- `pending` — สมัครแล้วรอเจ้าหน้าที่อนุมัติ ล็อกอินไม่ได้
- `approved` — ใช้งานได้ปกติ
- `retired` — ระบบระงับให้เองเมื่อบัญชีอายุครบ 365 วัน ไม่ต้องมีคนไปกด

### 4.3 ตาราง `checkin_logs` — ประวัติเข้า-ออก (6 คอลัมน์)

| คอลัมน์ | ชนิด | ข้อกำหนด | ความหมาย |
|---|---|---|---|
| `log_id` | INT | **PK**, AUTO_INCREMENT | รหัสหลัก **และเป็นลำดับที่ใช้จับคู่เข้า-ออก** |
| `user_id` | INT | NOT NULL, **FK** → `users` | ใครเป็นเจ้าของเหตุการณ์นี้ |
| `timestamp` | DATETIME | NOT NULL ค่าเริ่มต้น NOW() | เวลาที่เกิดเหตุการณ์ |
| `type` | ENUM | `in`,`out` NOT NULL | เข้า หรือ ออก |
| `planned_checkout_at` | DATETIME | ว่างได้ | เวลาที่ตั้งใจจะออก **บันทึกตอนเช็คอินเท่านั้น** ว่าง = "จนกว่าจะปิด" |
| `checkout_source` | ENUM | `manual`,`auto`,`admin_forced` ว่างได้ | ใครบันทึกการออก **บันทึกตอนเช็คเอาต์เท่านั้น** |

**Index**
- `idx_checkin_logs_timestamp (timestamp)` — ทุกรายงานกรองด้วยช่วงเวลา
- `idx_checkin_logs_user_time (user_id, timestamp)` — ใช้กับการดูประวัติรายคนและการกวาดปิดอัตโนมัติ

**`checkout_source` แต่ละค่าหมายถึง**
- `manual` — นักศึกษากดปุ่มเอง
- `auto` — ระบบปิดให้เมื่อเลยเวลาที่ตั้งใจไว้ หรือเลยเวลาปิดห้องสมุด
- `admin_forced` — เจ้าหน้าที่กดบังคับปิดให้

### 4.4 ตาราง `login_attempts` — กันเดารหัสผ่าน (4 คอลัมน์)

| คอลัมน์ | ชนิด | ข้อกำหนด | ความหมาย |
|---|---|---|---|
| `id` | INT | **PK**, AUTO_INCREMENT | รหัสหลัก |
| `ip` | VARCHAR(45) | NOT NULL | IP ที่พยายามล็อกอิน (45 ตัวเพื่อรองรับ IPv6) |
| `username` | VARCHAR(50) | NOT NULL ค่าเริ่มต้น `''` | ชื่อผู้ใช้ที่ถูกลองเดารหัส |
| `attempted_at` | DATETIME | NOT NULL ค่าเริ่มต้น NOW() | เวลาที่ลอง |

**บันทึกเฉพาะการล็อกอินที่ล้มเหลวเท่านั้น** และ**คอลัมน์ `username` คือหัวใจของตารางนี้** — ทั้งวิทยาลัยออกเน็ตผ่าน IP สาธารณะเดียวกัน ถ้านับแต่ IP คนพิมพ์รหัสผิดคนเดียวจะล็อกทุกคนออกจากระบบพร้อมกัน การเก็บชื่อผู้ใช้ด้วยทำให้จำกัดได้เป็นรายบัญชี

**ตารางนี้ไม่มี foreign key ไปไหนเลย และตั้งใจให้เป็นแบบนั้น** — มันบันทึกการ *พยายาม* ล็อกอิน ซึ่งรวมถึงชื่อผู้ใช้ที่ไม่มีอยู่จริง ถ้าผูก FK กับ `users` การเดารหัสด้วยชื่อมั่วจะบันทึกไม่ได้ ทั้งที่นั่นคือกรณีที่ต้องจับให้ได้ที่สุด

**Index**
- `idx_login_attempts_ip_time (ip, attempted_at)`
- `idx_login_attempts_ip_user_time (ip, username, attempted_at)`

### 4.5 ตาราง `app_settings` — ค่าตั้งค่าแบบ key/value (4 คอลัมน์)

ตอนนี้ใช้เก็บประกาศจากเจ้าหน้าที่ที่แสดงบนหน้าหลักนักศึกษา

| คอลัมน์ | ชนิด | ความหมาย |
|---|---|---|
| `setting_key` | VARCHAR(50) **PK** | ชื่อค่า — ใช้อยู่ 2 คีย์: `announcement_text`, `announcement_enabled` |
| `setting_value` | TEXT | ค่าที่เก็บ (ข้อความประกาศ หรือ `1`/`0`) |
| `updated_at` | DATETIME | เวลาที่แก้ล่าสุด |
| `updated_by` | VARCHAR(50) | ชื่อผู้ใช้ของแอดมินที่แก้ล่าสุด เก็บเป็นข้อความ ไม่ใช่ FK เพื่อให้ประกาศอยู่ต่อได้แม้บัญชีนั้นถูกลบ |

`src/handlers/announcement_handlers.php` สร้างตารางนี้เองด้วย `CREATE TABLE IF NOT EXISTS` ตอนใช้งานครั้งแรก เพราะการนำขึ้นเซิร์ฟเวอร์อัปแต่ไฟล์ ไม่ได้รันไฟล์ `.sql` ให้ ถ้าบัญชีฐานข้อมูลไม่มีสิทธิ์สร้างตาราง ระบบจะถือว่า "ไม่มีประกาศ" แทนการพังทั้งหน้า

### 4.6 ตาราง `schema_migrations` — ตารางของระบบ (2 คอลัมน์)

ไม่ได้อยู่ใน `schema.sql` แต่ถูกสร้างโดยไฟล์ migration

| คอลัมน์ | ชนิด | ความหมาย |
|---|---|---|
| `name` | VARCHAR(191) **PK** | ชื่อไฟล์ migration ที่รันไปแล้ว |
| `applied_at` | DATETIME | วันเวลาที่รัน |

มีหน้าที่กันไม่ให้ migration ที่บวกเวลา +7 ชั่วโมง ถูกรันซ้ำจนเวลาเพี้ยนสะสมไปเรื่อยๆ

### 4.7 ความสัมพันธ์

```
students ──1:N──> users ──1:N──> checkin_logs

login_attempts     (อิสระ ไม่ผูกกับตารางใด)
app_settings       (อิสระ ค่าตั้งค่าของระบบ)
schema_migrations  (อิสระ ตารางของระบบ)
```

`users.student_id` ไม่มี UNIQUE constraint ในทางเทคนิคจึงเป็น 1:N (นักศึกษา 1 คนมีบัญชีได้หลายบัญชี) แม้ในทางปฏิบัติจะใช้แบบ 1:1

---

## 5. ระบบยืนยันตัวตนและสิทธิ์

### 5.1 การจัดการ session

ไฟล์ `src/auth.php` มี 3 ฟังก์ชัน

| ฟังก์ชัน | หน้าที่ |
|---|---|
| `bootstrap_session()` | ตั้งค่า cookie ของ session ต้องรันก่อนมีการส่งข้อมูลออกใดๆ |
| `require_login()` | ถ้ายังไม่ล็อกอิน จบ request ทันทีด้วย 401 |
| `require_admin()` | ถ้าไม่ใช่ผู้ดูแล จบ request ทันทีด้วย 403 |

ค่า cookie ปรับตาม `APP_ENV` — `dev` ใช้ผ่าน http ได้เพื่อพัฒนาในเครื่อง ส่วน `prod` บังคับ Secure และ SameSite ที่เข้มขึ้น

### 5.2 ยามเฝ้าหน้าเว็บ (`partials/guard.php`)

ทุกหน้าที่ต้องล็อกอินจะประกาศตัวแปรก่อนแล้วเรียก guard

```php
$requireAdmin = false;              // หรือ true
require __DIR__ . '/partials/guard.php';
```

guard เด้งไปหน้าล็อกอินถ้ายังไม่ได้ล็อกอิน และเด้งกลับถ้าสิทธิ์ไม่พอ **ป้องกันตั้งแต่ฝั่งเซิร์ฟเวอร์ ไม่ใช่แค่ซ่อนปุ่มด้วย JavaScript**

### 5.3 รหัสผ่าน

| กติกา | ค่า | เหตุผล |
|---|---|---|
| สั้นสุด | 8 **ตัวอักษร** | นับด้วย `mb_strlen` ไม่ใช่ `strlen` — ถ้านับไบต์ รหัสไทย 4 ตัวจะผ่านได้ เพราะอักษรไทยตัวละ 3 ไบต์ |
| ยาวสุด | 72 **ไบต์** | bcrypt อ่านแค่ 72 ไบต์แรกแล้วทิ้งที่เหลือเงียบๆ ถ้ายอมให้ยาวกว่านี้ รหัสสองอันที่ขึ้นต้นเหมือนกันจะปลดล็อกบัญชีเดียวกันได้ ระบบจึงปฏิเสธแทนที่จะตัดทิ้ง |
| วิธีเข้ารหัส | bcrypt (`PASSWORD_BCRYPT`) | ไม่เก็บรหัสจริงลงฐานข้อมูลเลย |

เมื่อเปลี่ยนรหัสผ่านสำเร็จ ระบบเรียก `session_regenerate_id(true)` ทันที เพราะการเปลี่ยนรหัสมักเกิดจาก "สงสัยว่ามีคนเข้าบัญชีเรา" — session เก่าที่ยังค้างอยู่จะถือรหัสที่ตายแล้ว

### 5.4 การจำกัดการเดารหัสผ่าน (`src/rate_limit.php`)

| ค่าคงที่ | ค่า | ความหมาย |
|---|---|---|
| `LOGIN_FAILURE_WINDOW_MINUTES` | 1 | นับย้อนหลัง 1 นาที |
| `LOGIN_MAX_FAILURES_PER_ACCOUNT` | 5 | บัญชีเดียวผิดเกิน 5 ครั้งใน 1 นาที = ถูกกัน |
| `LOGIN_MAX_FAILURES_PER_IP` | 50 | IP เดียวผิดเกิน 50 ครั้งใน 1 นาที = ถูกกัน |

ตัวเลข 2 ชั้นนี้มีเหตุผล — ทั้งวิทยาลัยใช้ IP สาธารณะเดียวกัน ถ้ากันที่ IP อย่างเดียวและตั้งไว้ต่ำ นักศึกษาคนหนึ่งพิมพ์ผิดจะทำให้ทั้งโรงเรียนล็อกอินไม่ได้ ชั้นรายบัญชีจึงเป็นด่านหลัก ชั้น IP ตั้งสูงไว้กันการยิงถล่มเท่านั้น **บันทึกเฉพาะครั้งที่ล้มเหลว** การล็อกอินสำเร็จไม่ถูกนับ

### 5.5 การจัดการเวลา

ระบบตั้ง timezone เป็น `Asia/Bangkok` ตั้งแต่บรรทัดแรกๆ ของ `index.php` **ก่อน** ที่จะมีการใช้ `DateTime` ใดๆ เพราะค่าเริ่มต้นของ PHP บนเครื่องพัฒนาคือ `Europe/Berlin` ซึ่งไม่ตรงกับ `NOW()` ของ MySQL ที่ตามนาฬิกาเครื่อง ถ้าไม่ตั้ง เวลาที่ PHP คำนวณกับเวลาที่ฐานข้อมูลบันทึกจะต่างกันหลายชั่วโมง

---

## 6. หน้าเว็บทั้งหมด

| หน้า | ชื่อบนแท็บ | ใครเข้าได้ |
|---|---|---|
| `index.php` | — | ทุกคน (front controller ไม่ใช่หน้าเว็บ) |
| `login.php` | เข้าสู่ระบบ | ทุกคน |
| `signup.php` | สมัครสมาชิก | ทุกคน |
| `dashboard.php` | หน้าหลัก | นักศึกษาที่ล็อกอินแล้ว |
| `profile.php` | โปรไฟล์ | นักศึกษาที่ล็อกอินแล้ว |
| `admin-dashboard.php` | ภาพรวม | **เจ้าหน้าที่เท่านั้น** |
| `admin-members.php` | ทำเนียบสมาชิก | **เจ้าหน้าที่เท่านั้น** |
| `admin-active.php` | กำลังใช้งานอยู่ | **เจ้าหน้าที่เท่านั้น** |
| `admin-logs.php` | ประวัติการเช็คชื่อ | **เจ้าหน้าที่เท่านั้น** |

---

### 6.1 `login.php` — หน้าเข้าสู่ระบบ

**ส่วนประกอบ** แบนเนอร์หัวเรื่องพร้อมโลโก้ (`partials/lib-banner.php`), ช่องรหัสนักศึกษา, ช่องรหัสผ่านพร้อมปุ่มดวงตาเปิด/ปิดการมองเห็น, ปุ่มเข้าสู่ระบบ, ลิงก์ไปหน้าสมัคร, ปุ่มสลับธีมสว่าง/มืด, footer ลิขสิทธิ์

**แบนเนอร์** ความสูงถูกตรึงไว้ที่ ~190px ด้วย `min-height` บน `.lib-banner .content` ไม่ใช่ปล่อยให้ไหลตาม padding เพื่อให้ฟอร์มล็อกอินยังอยู่เหนือ fold บนมือถือเสมอ บรรทัด "ห้องสมุด วิทยาลัยเทคนิคนครนายก" เป็นข้อความที่ใหญ่ที่สุดในหน้า

**ปุ่มดูรหัสผ่าน** สลับ `type` ของ `<input>` เดิมระหว่าง `password` กับ `text` ไม่ได้สร้างช่องใหม่ ค่าที่พิมพ์ไว้จึงไม่หายและตัวจัดการรหัสผ่านของเบราว์เซอร์ยังเห็นเป็นช่องเดิม

**การทำงาน** (`assets/js/login.js`)
1. ส่ง `POST /login` พร้อม username + password
2. สำเร็จ → อ่าน `role` ที่ตอบกลับ แล้วส่งไปหน้าตามบทบาท — `admin` ไป `admin-dashboard`, `student` ไป `dashboard` (ฟังก์ชัน `landingPathForRole`)
3. ล้มเหลว → แสดงข้อความผิดพลาดใต้ฟอร์ม (`showLoginError`)

---

### 6.2 `signup.php` — หน้าสมัครสมาชิก

**ช่องกรอกทั้งหมด**

| ช่อง | id | หมายเหตุ |
|---|---|---|
| รหัสนักศึกษา | `signup-student-id` | ตัวเลข 5–20 หลัก ใช้เป็น username |
| คำนำหน้า | `signup-prefix` | นาย / นาง / นางสาว |
| เพศ | `signup-gender` | ชาย / หญิง |
| ชื่อ | `signup-first-name` | |
| นามสกุล | `signup-last-name` | |
| แผนกวิชา | `signup-department` | เลือกจาก 14 แผนก |
| ระดับชั้น | `signup-level` | ปวช. / ปวส. |
| ชั้นปี | `signup-year-level` | ตัวเลือกเปลี่ยนตามระดับชั้น |
| รหัสผ่าน | `signup-password` | อย่างน้อย 8 ตัวอักษร |
| ยืนยันรหัสผ่าน | `signup-confirm-password` | ต้องตรงกับช่องบน |

**การทำงาน** (`assets/js/signup.js`) — `populateSelect()` เติมตัวเลือกลง dropdown, ตัวเลือกชั้นปีเปลี่ยนตามระดับชั้น (ปวช. 3 ปี / ปวส. 2 ปี), เมื่อสมัครเสร็จขึ้นกล่องผลลัพธ์ (`showSignupResultModal`)

**สำคัญ:** บัญชีใหม่มีสถานะ `pending` **ล็อกอินยังไม่ได้** จนกว่าเจ้าหน้าที่จะเปลี่ยนเป็น `approved`

---

### 6.3 `dashboard.php` — หน้าหลักนักศึกษา

**ส่วนประกอบจากบนลงล่าง**
1. **แถบประกาศจากเจ้าหน้าที่** — ข้อความมาจาก `GET /announcement` แอดมินแก้ได้จากหน้าภาพรวม ถ้าไม่มีประกาศหรือถูกปิดไว้ ทั้งบล็อกจะถูกซ่อน
2. **ป้ายสถานะ** — จุดสีพร้อมข้อความ มี 3 สถานะ: เขียว(กะพริบ) = อยู่ในห้องสมุด, น้ำเงิน = เช็คเอาต์แล้ววันนี้ พร้อมเวลาที่ออก, ส้ม = ยังไม่ได้เช็คอินวันนี้
3. **ปุ่มวงกลมใหญ่กลางจอ** — ปุ่มเดียวที่สลับเช็คอิน/เช็คเอาต์เอง
4. **นาฬิกานับเวลา** — "อยู่มาแล้ว HH:MM:SS" เดินทุกวินาที
5. **ปุ่มขอเวลาเพิ่ม** 30 / 60 นาที
6. **การ์ดใช้งานล่าสุด** พร้อมปุ่ม "ดูทั้งหมด"

**หน้าต่างเลือกเวลาออก (เปิดตอนกดเช็คอิน)** มี 2 แท็บ
- **เลือกเวลาที่จะออก** — วงล้อเลื่อนเลือกชั่วโมง:นาที ไม่ใช้ `<input type="time">` เพราะบนมือถือมันเปิด overlay ของระบบปฏิบัติการทับ
- **เลือกจำนวนชั่วโมง** — ปุ่มสำเร็จรูป
- ปุ่มลัด "จนกว่าจะปิด"

**แจ้งเตือนใกล้ถึงเวลา** แถบสีส้มเด้งด้านล่างจอพร้อมปุ่มขอเวลาเพิ่มทันที แถบนี้ผูกกับสถานะจริง ไม่ใช่กับการกดปุ่ม — ถ้าการเข้าใช้ถูกปิดจากฝั่งเซิร์ฟเวอร์ (รอบปิดอัตโนมัติ หรือเจ้าหน้าที่กดปิดให้) แถบจะหายไปเองในการรีเฟรชเบื้องหลังรอบถัดไป

**วันที่ห้องสมุดปิด** เสาร์-อาทิตย์ (ตั้งค่าที่ `LIBRARY_OPEN_DAYS`) ปุ่มเช็คอินจะถูกปิดไว้และข้อความใต้ปุ่มเปลี่ยนเป็น "วันนี้ห้องสมุดปิดทำการ เปิดให้บริการวันจันทร์-ศุกร์" คนที่ยังค้างอยู่ในห้องสมุดจากวันทำการยังกดเช็คเอาต์ได้ตามปกติ และเซิร์ฟเวอร์ปฏิเสธการเช็คอินซ้ำอีกชั้นหนึ่งด้วย ไม่ได้พึ่งการปิดปุ่มอย่างเดียว

**การเข้าสู่ระบบไม่ถูกบล็อกในวันหยุด** — บล็อกเฉพาะการเช็คอิน นักศึกษายังเปิดดูประวัติของตัวเองได้ และเจ้าหน้าที่ยังทำรายงานย้อนหลังได้

**กลไกรีเฟรชเบื้องหลัง**
- รีเฟรชทุก 60 วินาที เพื่อรับรู้ว่าระบบปิดการเข้าใช้ให้แล้ว
- รีเฟรชทันทีเมื่อกลับมาที่แท็บ (`visibilitychange`) — เคสที่พบบ่อยสุดคือมือถือล็อกจอไว้นาน
- 3 ด่านกันเรียกซ้ำ: ข้ามถ้าหน้าต่างเช็คอินเปิดอยู่, ข้ามถ้ากำลังส่งคำสั่ง, ถ้าพลาดก็เงียบไม่เด้ง error
- หยุด timer เมื่อออกจากหน้า และ re-arm เมื่อกลับมา

---

### 6.4 `profile.php` — หน้าโปรไฟล์นักศึกษา

**อ่านอย่างเดียว** (ต้องให้เจ้าหน้าที่แก้ให้) — รหัสนักศึกษา, ชื่อ-สกุล, แผนกวิชา, ระดับชั้น, ชั้นปี

**แก้ได้: เปลี่ยนรหัสผ่าน** — ช่องรหัสผ่านปัจจุบัน (ยืนยันตัวตน) + ช่องรหัสผ่านใหม่ (อย่างน้อย 8 ตัวอักษร)

มีเมนูข้าง (`student-sidebar.php`) ที่บนมือถือยุบเป็นปุ่มขีดสามขีด

---

### 6.5 `admin-dashboard.php` — ภาพรวมสำหรับเจ้าหน้าที่

**ช่องพิมพ์ประกาศถึงนักศึกษา** อยู่บนสุดของส่วนเนื้อหา ใต้แถบตัวเลขสรุป
- ช่องพิมพ์ข้อความ ยาวได้ไม่เกิน 500 ตัวอักษร มีตัวนับอยู่มุมขวา
- สวิตช์ "แสดงประกาศนี้บนหน้านักศึกษา" — ปิดไว้ได้โดยข้อความยังถูกเก็บอยู่
- บรรทัดล่างบอกว่าใครแก้ล่าสุดเมื่อไหร่
- บันทึกด้วย `POST /admin/announcement` เก็บลงตาราง `app_settings`
- ระบบไม่ยอมให้เปิดแสดงประกาศที่เป็นข้อความว่าง และไม่รับอักขระ `<` `>`

**แถบตัวเลขสรุป 4 กล่อง** แต่ละกล่องมีปุ่ม ⓘ กดแล้วกางคำอธิบาย

| กล่อง | คำอธิบายที่กางออกมา |
|---|---|
| จำนวนครั้งที่เข้าใช้ | จำนวนครั้งที่มีการเข้าใช้ห้องสมุดทั้งหมดในช่วงเวลาที่เลือก |
| จำนวนนักศึกษาที่เข้าใช้ | จำนวนผู้เข้าใช้ที่ไม่ซ้ำกัน โดยนับแต่ละคนเพียง 1 คน |
| เฉลี่ยการเข้าใช้ต่อวัน | จำนวนครั้งที่เข้าใช้ห้องสมุดโดยเฉลี่ยในแต่ละวัน |
| ผู้ที่อยู่ในห้องสมุดขณะนี้ | จำนวนผู้ที่เช็กอินแล้วและยังไม่ได้เช็กเอาต์ออก |

เปิดได้ทีละกล่อง เพราะบนมือถือการ์ดเรียง 2×2 ถ้าเปิดสองกล่องพร้อมกันจะดันแถวล่างตกจอ

**ตัวกรองช่วงเวลา** — เดือนนี้ / 7 วันล่าสุด / วันนี้

**กราฟและตาราง 4 ส่วน**
1. **แนวโน้มการเข้าใช้** — กราฟแท่งรายวัน คลิกแท่งเพื่อดูรายละเอียดวันนั้น (เปิดหน้าต่างแยกตามแผนก แล้วเจาะลงไปดูรายชื่อนักศึกษาได้) — การกดแท่งกราฟมีเฉพาะหน้านี้ซึ่งเป็นหน้าจอ ส่วนหน้ารายงานที่ทำไว้สั่งพิมพ์ไม่มี
2. **แผนกที่เข้าใช้มากที่สุด** — แถบเรียงอันดับ พร้อมข้อความสรุปว่าวันไหนและช่วงเวลาไหนคนเยอะสุด
3. **ช่วงเวลาที่คนเข้าใช้มากที่สุด** — กราฟแท่งรายชั่วโมง 0–23 น. จำนวนป้ายบนแกนคำนวณจากความกว้างจอจริง
4. **วันในสัปดาห์ที่คนเข้าใช้** — แถบแนวนอน จันทร์–อาทิตย์

**ปุ่มพิมพ์รายงาน** เปิดหน้าเลือกรายงานในแท็บใหม่

**การรีเฟรช** ตัวเลข "อยู่ในห้องสมุดขณะนี้" คำนวณฝั่งเบราว์เซอร์จากข้อมูลชุดเดียว จึงต้องดึงใหม่ทุก 20 วินาที ไม่งั้นค้าง

---

### 6.6 `admin-members.php` — ทำเนียบสมาชิก

**ตัวกรอง** ค้นหาด้วยรหัส/ชื่อ, แผนกวิชา, ระดับชั้น, ชั้นปี, สถานะบัญชี

**แต่ละแถวแสดง** ชื่อ-สกุล, รหัสนักศึกษา, แผนก · ระดับ · ชั้นปี, วันที่สมัคร, ป้ายสถานะ

**การเรียงลำดับ** เรียงตามรหัสนักศึกษา ไม่ใช่ชื่อ ก-ฮ เพราะเจ้าหน้าที่ค้นจากรหัสที่อยู่ในเอกสาร คำสั่งเรียงคือ `ORDER BY LENGTH(student_id), student_id` — ต้องเรียงตามความยาวก่อน เพราะคอลัมน์เป็น VARCHAR ถ้าเรียงแบบข้อความล้วนและรหัสยาวไม่เท่ากัน รหัสสั้นกว่าจะไปตกอยู่ผิดตำแหน่ง (หน้าเลือกนักศึกษาในรายงาน และตารางรายวัน/รายเดือน ใช้การเรียงแบบเดียวกัน)

**ปุ่มในแต่ละแถว**
- **แก้ไข** — เปิดหน้าต่างแก้ไขข้อมูล
- **รีเซ็ตรหัสผ่าน** — ระบบสุ่มรหัสชั่วคราวแล้วแสดงบนจอให้เจ้าหน้าที่อ่านให้นักศึกษาฟัง (ไม่มีอีเมลในระบบ) รหัสที่สุ่มตัดตัวอักษรที่สับสนออก เช่น 0/O และ 1/l/I เพราะต้องอ่านออกเสียง

**หน้าต่างแก้ไขสมาชิก แก้ได้** คำนำหน้า, เพศ, ชื่อ, นามสกุล, แผนกวิชา, ระดับชั้น, ชั้นปี, ห้องเรียน, สถานะบัญชี, บทบาท (นักศึกษา/ผู้ดูแล) และปุ่มลบสมาชิก

**ด่านความปลอดภัยสำคัญ** ระบบต้องไม่มีวันไปถึงสภาพที่ไม่มีใครดูแลได้ การลดบทบาท การระงับบัญชี และการลบ ทั้ง 3 อย่างจึงถามก่อนเสมอว่ายังมีผู้ดูแลคนอื่นเหลืออยู่ไหม (`admin_count_other_admins`)

**แบ่งหน้า** ครั้งละ 10 รายการ

---

### 6.7 `admin-active.php` — ใครอยู่ในห้องสมุดตอนนี้

**ตัวเลขสรุป** จำนวนคนที่อยู่ตอนนี้ และคนที่อยู่นานที่สุด

**ตาราง** ชื่อ, รหัสนักศึกษา, แผนก, เวลาที่เช็คอิน, อยู่มาแล้วกี่นาที, ปุ่มบังคับเช็คเอาต์

**จุดที่ควรรู้**
- ตัวเลข "อยู่มาแล้ว" เดินเองทุกวินาทีโดยไม่ดึงข้อมูลใหม่ (`tickDurations`) ส่วนการดึงข้อมูลจริงทำทุก 20 วินาที
- ปุ่มบังคับเช็คเอาต์เขียนแถว `out` ปกติ แต่ติดป้าย `checkout_source = 'admin_forced'` เพื่อแยกออกจากที่นักศึกษากดเองและที่ระบบปิดให้

---

### 6.8 `admin-logs.php` — ประวัติการเช็คชื่อทั้งหมด

**ตัวเลขสรุป 3 กล่อง** รายการทั้งหมด, อยู่ในห้องสมุดตอนนี้, ระยะเวลาเฉลี่ยต่อครั้ง

**ตัวกรอง** ค้นหาด้วยชื่อ/รหัส/แผนก, ประเภท (เช็คอิน/เช็คเอาต์), วันที่, ปุ่มลัด "เดือนนี้"

**ตาราง** วันเวลา, ชื่อนักศึกษา, รหัสนักศึกษา, แผนกวิชา, ประเภท

**แบ่งหน้า** ครั้งละ 10 รายการ — ฐานข้อมูลจริงมีมากกว่า 14,000 แถว การวาดทั้งหมดลงหน้าเดียวทำให้เบราว์เซอร์ช้าและอ่านไม่ไหว

---

### 6.9 หน้าต่างประวัติการเข้าใช้ (ของนักศึกษา)

เปิดจากเมนูบัญชี หรือปุ่ม "ดูทั้งหมด" ในหน้าหลัก

**ตัวกรองช่วงเวลา** dropdown เดียว — ทั้งหมด / 7 วันล่าสุด / 30 วันล่าสุด / เดือนนี้ / กำหนดเอง
เลือก "กำหนดเอง" แล้วช่องเลือกวันที่ 2 ช่องจึงกางออกมา ปกติซ่อนไว้เพื่อไม่ให้กินพื้นที่จอมือถือ

**แถบสรุปของช่วงที่เลือก** จำนวนครั้งที่เข้าใช้, เวลารวม, เฉลี่ยต่อครั้ง

**รายการ** จัดกลุ่มตามเดือน หัวข้อเดือนติดหนึบขณะเลื่อน แต่ละแถวมี บล็อกวันที่ (เลขวัน + ชื่อวัน) → เวลาเข้า → เส้นเวลา → เวลาออก → ระยะเวลาที่อยู่

**สถานะพิเศษ**
- ยังไม่ออก → เส้นประสีส้ม ข้อความ "ยังอยู่ในห้องสมุด" ป้าย "กำลังนับ"
- ระบบปิดให้ → หมายเหตุ "ระบบบันทึกให้"
- เจ้าหน้าที่ปิดให้ → หมายเหตุ "เจ้าหน้าที่บันทึกให้"

**แบ่งหน้า** ครั้งละ 10 ครั้ง พร้อมข้อความ "แสดง 1–10 จาก N รายการ"

---

## 7. ระบบรายงาน

เข้าจากปุ่ม "พิมพ์รายงาน" ในหน้าภาพรวม จะเปิดหน้าเลือกรายงาน (`/admin/reports/print`) **เจ้าหน้าที่เท่านั้น** ทุกรายงานเรียก `require_admin()`

### 7.1 รายงานทั้ง 8 แบบ

| # | รายงาน | เส้นทาง | เนื้อหา |
|---|---|---|---|
| 1 | รายงานแบบแดชบอร์ด | `/admin/reports/print/dashboard` | สรุปภาพรวมแบบ KPI การ์ด + กราฟ + สัดส่วนเพศ จบในหน้าเดียว |
| 2 | สรุปสำหรับผู้บริหาร | `/admin/reports/print/executive` | ภาพรวมระดับผู้บริหาร มี % เทียบกับเดือนก่อนหน้า |
| 3 | รายงานภาคเรียน | `/admin/reports/print/semester` | สรุปผลการใช้ห้องสมุดตลอดภาคเรียน |
| 4 | รายงานประจำวัน | `/admin/reports/print/daily` | รายการเช็คชื่อของวันที่เลือก |
| 5 | รายงานสรุปรายเดือน | `/admin/reports/print/monthly` | สรุปรายเดือน + เปรียบเทียบเดือน/สัปดาห์ |
| 6 | รายงานสรุปรายแผนกวิชา | `/admin/reports/print/department` | แยกตามแผนก + เปรียบเทียบเดือนก่อนหน้า + ตารางไขว้ตามระดับชั้น |
| 7 | เปรียบเทียบช่วงเวลา | `/admin/reports/print/compare` | เทียบสองช่วงเวลาที่เลือกเอง |
| 8 | รายงานรายบุคคล | `/admin/reports/print/student` | ค้นหานักศึกษาแล้วดูประวัติรายคน |

### 7.2 ตัวกรองร่วมของทุกรายงาน

ทุกรายงานใช้ชุดตัวกรองเดียวกัน สร้างจากฟังก์ชัน `build_filter_clause()` ที่เดียว จึงกรองเหมือนกันทุกใบ ไม่มีทางเพี้ยนจากกัน

- แผนกวิชา (`department`)
- ระดับชั้น (`level`)
- ชั้นปี (`year_level`)
- ภาคเรียน (`semester`)
- ปีการศึกษา (`academic_year`)
- ช่วงวันที่ หรือ เลือกเป็นเดือน

ตัวเลือกใน dropdown ดึงจากค่าที่มีอยู่จริงในฐานข้อมูล (`distinct_student_values()`) ไม่ใช่รายการที่พิมพ์ตายไว้ซึ่งจะเก่าตามข้อมูล

**หมายเหตุเรื่องเดือนกับช่วงวันที่** ทั้งสองอย่างบอกช่วงเวลาเหมือนกัน ระบบจึงต้องเลือกว่าอันไหนชนะ กติกาคือ **เดือนที่ผู้ใช้เลือกมาเองชนะเสมอ** และเมื่อเดือนชนะ ช่องวันที่จะถูกล้างทิ้ง เพื่อไม่ให้ฟอร์มแสดงช่วงวันที่ที่รายงานไม่ได้ใช้จริง

### 7.3 แถบเครื่องมือของหน้ารายงาน

| ปุ่ม | หน้าที่ |
|---|---|
| หน้าหลัก | กลับไปหน้าภาพรวมของเจ้าหน้าที่ |
| ← เลือกเทมเพลตอื่น | กลับไปหน้าเลือกรายงาน |
| **ดาวน์โหลด PDF** | ปุ่มหลัก สร้างไฟล์ PDF จริง |
| ⋯ เพิ่มเติม | ยุบ พิมพ์หน้านี้ / ดาวน์โหลด CSV / ดาวน์โหลด Excel / ตั้งค่าการพิมพ์ ไว้ข้างใน |

เมนู "เพิ่มเติม" ใช้แท็ก `<details>` ของ HTML จึงทำงานได้โดยไม่ต้องใช้ JavaScript เลย และรอดในขั้นตอนสร้าง PDF ที่สคริปต์ถูกถอดออก

### 7.4 การสร้าง PDF ทำงานอย่างไร

นี่คือส่วนที่ซับซ้อนที่สุดของระบบรายงาน

**ปัญหา** กราฟบนหน้าจอทุกอันสร้างจาก `<div>` ที่กำหนดความสูงเป็นเปอร์เซ็นต์ ซึ่ง mPDF อ่านไม่เข้าใจ ผลคือรายงาน 1 หน้าเคยบานเป็น **50–300 หน้าเปล่า**

**วิธีแก้** แยกทางเดินสองสาย
1. **บนหน้าจอ** ใช้ `<div>` กราฟตามปกติ
2. **ใน PDF** ฟังก์ชัน `render_pdf_bar_chart()` วาดกราฟเป็นรูป **PNG จริงด้วย GD** แล้วส่งเป็น data URI ให้ mPDF แค่วางรูป ไม่ต้องคำนวณ layout
3. CSS ของ PDF เป็นคนละชุดกับ CSS ของหน้าจอ (ไม่ใช้ `$extraStyle` ของแต่ละรายงานเลย) และสั่งซ่อน `<div>` กราฟเวอร์ชันหน้าจอทิ้ง เพื่อไม่ให้ตัวเลขชุดเดียวกันโผล่สองรอบ

**ฟอนต์ใน PDF** mPDF อ่าน `.woff2` ไม่ได้ จึงต้องมีไฟล์ `.ttf` แยกไว้ในโฟลเดอร์ `fonts/` ต่างหากจากฟอนต์ของเว็บ ตั้ง `ibmplexsansthai` เป็นฟอนต์เริ่มต้น มี Sarabun เป็นตัวสำรอง

**สิ่งที่ยอมตัดทิ้งใน PDF** กราฟเส้นจิ๋ว (sparkline) และวงแหวนเปอร์เซ็นต์ — เป็นของประดับบนการ์ด ไม่ใช่ตัวข้อมูล

### 7.5 การส่งออก CSV และ Excel

ฟังก์ชัน `export_response()` ใช้ร่วมกันทุกรายงาน **ยกเว้นรายงานแดชบอร์ด** ซึ่งเป็นใบสรุปหน้าเดียว ไม่ใช่รายงานแบบแถวต่อรายการ

- **CSV** ใช้ฟังก์ชันในตัวของ PHP ล้วน
- **Excel** คือตาราง HTML ที่ตั้งนามสกุลเป็น `.xls` Excel เปิดได้ปกติ วิธีนี้ทำให้ไม่ต้องพึ่งไลบรารีเพิ่ม

---

## 8. API ทั้งหมด 30 เส้นทาง

ทุกเส้นทางผ่าน `public/index.php` ซึ่งเก็บแผนที่ `"METHOD path" => ชื่อฟังก์ชัน` ไว้แบบแบนๆ ถ้าไม่ตรงกับที่ลงทะเบียนไว้จะตอบ 404 ทันที

### 8.1 กลุ่มทั่วไป / ยืนยันตัวตน

| Method | เส้นทาง | ฟังก์ชัน | สิทธิ์ | หน้าที่ |
|---|---|---|---|---|
| GET | `/` | `handle_root_redirect` | ทุกคน | เด้งไปหน้าล็อกอิน |
| POST | `/register` | `handle_register` | ทุกคน | สมัครสมาชิก สร้างสถานะ `approved` ใช้งานได้ทันที |
| POST | `/login` | `handle_login` | ทุกคน | เข้าสู่ระบบ ผ่านด่านจำกัดการเดารหัส |
| POST | `/logout` | `handle_logout` | ล็อกอินแล้ว | ออกจากระบบ ทำลาย session |
| GET | `/library-info` | `handle_library_info` | ล็อกอินแล้ว | เวลาปิดห้องสมุด วันเปิดทำการ และสถานะเปิด/ปิดของวันนี้ |
| GET | `/announcement` | `handle_announcement` | ล็อกอินแล้ว | ประกาศจากเจ้าหน้าที่ที่แสดงบนหน้าหลักนักศึกษา |
| POST | `/admin/announcement` | `handle_admin_announcement_save` | ผู้ดูแล | บันทึก/เปิด-ปิดประกาศ |

### 8.2 กลุ่มของตัวเอง (นักศึกษา)

| Method | เส้นทาง | ฟังก์ชัน | หน้าที่ |
|---|---|---|---|
| GET | `/me` | `handle_me` | ข้อมูลบัญชีและโปรไฟล์ของตัวเอง |
| POST | `/profile/change-password` | `handle_change_password` | เปลี่ยนรหัสผ่าน แล้วออก session ใหม่ |
| GET | `/me/history` | `handle_my_history` | ประวัติแบบดิบ **แถวละเหตุการณ์** ใช้ตอบคำถาม "ตอนนี้ฉันอยู่ข้างในไหม" |
| GET | `/me/visits` | `handle_my_visits` | ประวัติแบบ **แถวละการมา 1 ครั้ง** พร้อมตัวกรองช่วงวันที่และยอดสรุป |
| POST | `/checkin` | `handle_checkin` | ปุ่มสลับเช็คอิน/เช็คเอาต์ |
| POST | `/checkin/extend` | `handle_checkin_extend` | ขอเวลาเพิ่ม |

**ความต่างของ `/me/history` กับ `/me/visits`** ตัวแรกคืนแถวดิบตามที่เก็บในฐานข้อมูล ตัวหลังจับคู่ `in` กับ `out` ให้แล้วด้วย window function `LEAD()` เพราะคนอ่านประวัติตัวเองคิดเป็น "การมา 1 ครั้ง" ไม่ได้คิดเป็นเหตุการณ์แยกกัน 2 อัน

**พารามิเตอร์ของ `/me/visits`**

| พารามิเตอร์ | ค่า | ความหมาย |
|---|---|---|
| `limit` | 1–100 ค่าเริ่มต้น 10 | จำนวนต่อหน้า |
| `offset` | ≥ 0 | ข้ามกี่รายการ |
| `from` | YYYY-MM-DD | วันเริ่มต้น (นับทั้งวัน) |
| `to` | YYYY-MM-DD | วันสิ้นสุด (นับทั้งวัน) |

ค่าที่ตอบกลับมี `visits`, `total`, `summary` (จำนวนครั้ง / จำนวนครั้งที่ปิดแล้ว / นาทีรวม / นาทีเฉลี่ย), `range`, และ `bounds` (วันแรก–วันล่าสุดที่มีข้อมูล ใช้ล็อกช่วงของตัวเลือกวันที่)

**จุดสำคัญทางเทคนิค** ตัวกรองวันที่ถูกใช้กับผลลัพธ์ที่จับคู่แล้ว **ไม่ใช่ใช้ข้างใน subquery** เพราะ `LEAD()` ต้องมองเห็น log ทั้งก้อนถึงจะทำงานถูก ถ้ากรองก่อน คนที่เข้าวันสุดท้ายของช่วงแล้วออกวันถัดไปจะถูกรายงานว่า "ยังไม่ออก" ทั้งที่ออกไปแล้ว

### 8.3 กลุ่มผู้ดูแล — จัดการสมาชิก

| Method | เส้นทาง | ฟังก์ชัน | หน้าที่ |
|---|---|---|---|
| GET | `/admin/members` | `handle_admin_members` | รายชื่อสมาชิกทั้งหมดพร้อมข้อมูลนักศึกษา |
| POST | `/admin/members/update` | `handle_admin_member_update` | แก้ไขข้อมูลและสถานะบัญชี |
| POST | `/admin/members/role` | `handle_admin_member_role` | สลับบทบาทนักศึกษา ↔ ผู้ดูแล |
| POST | `/admin/members/delete` | `handle_admin_member_delete` | ลบสมาชิก |
| POST | `/admin/members/reset-password` | `handle_admin_reset_password` | สุ่มรหัสชั่วคราวให้ |

### 8.4 กลุ่มผู้ดูแล — การเข้าใช้

| Method | เส้นทาง | ฟังก์ชัน | หน้าที่ |
|---|---|---|---|
| GET | `/admin/active-now` | `handle_admin_active_now` | ใครอยู่ในห้องสมุดตอนนี้ |
| POST | `/admin/checkin/force-checkout` | `handle_admin_force_checkout` | บังคับเช็คเอาต์ให้ |
| GET | `/admin/reports` | `handle_admin_reports` | ข้อมูลดิบสำหรับหน้าภาพรวมและหน้าประวัติ |

### 8.5 กลุ่มรายงาน (คืนค่าเป็นหน้า HTML หรือไฟล์ PDF)

| Method | เส้นทาง | ฟังก์ชัน |
|---|---|---|
| GET | `/admin/reports/print` | `handle_report_select` |
| GET | `/admin/reports/print/daily` | `handle_report_daily` |
| GET | `/admin/reports/print/monthly` | `handle_report_monthly` |
| GET | `/admin/reports/print/department` | `handle_report_department` |
| GET | `/admin/reports/print/dashboard` | `handle_report_dashboard` |
| GET | `/admin/reports/print/executive` | `handle_report_executive` |
| GET | `/admin/reports/print/compare` | `handle_report_compare` |
| GET | `/admin/reports/print/semester` | `handle_report_semester` |
| GET | `/admin/reports/print/student` | `handle_report_student_lookup` |

เติม `?format=pdf` ต่อท้ายเพื่อให้คืนเป็นไฟล์ PDF แทนหน้าเว็บ

---

## 9. กลไกเบื้องหลัง

ระบบนี้ **ไม่มี cron และไม่มี background worker** งานเบื้องหลังทั้งหมดรันแทรกในทุก request ที่เข้ามา (`public/index.php` เรียกทันทีหลังเปิด session) เหตุผลคือปริมาณงานเล็กมาก คำสั่ง SQL ชุดเดียวก็จบ การตั้ง cron หรือ queue จริงจึงเป็นการเพิ่มของที่ต้องดูแลโดยไม่ได้อะไรกลับมา

### 9.1 `auto_checkout_sweep()` — ปิดการเข้าใช้ให้คนที่ลืมกด

ทำงานทุก request มองหาคนที่แถวล่าสุดเป็น `in` และเลยเวลาที่ควรออกไปแล้ว จากนั้นเขียนแถว `out` ให้ พร้อมติดป้าย `checkout_source = 'auto'`

**เวลาที่ควรออก** คำนวณจาก `GREATEST(COALESCE(planned_checkout_at, เวลาปิดห้องสมุดของวันนั้น), เวลาที่เช็คอิน)`

การครอบด้วย `GREATEST(..., เวลาที่เช็คอิน)` มีไว้กันกรณีที่เวลาออกจะตกก่อนเวลาเข้า ซึ่งเกิดได้ถ้าวันหนึ่งมีคนย้ายเวลาปิดห้องสมุดให้เร็วกว่าการเข้าใช้ที่ค้างอยู่

**หมายเหตุทางเทคนิค** คำสั่งนี้ใช้ตัวแปรเดียวกันสองตำแหน่ง (ทั้งใน SELECT และใน WHERE) แต่ PDO ปิด emulated prepares ไว้ prepared statement จริงจึงใช้ placeholder ชื่อเดียวซ้ำสองที่ไม่ได้ ต้องผูกคนละชื่อแต่ให้ค่าเดียวกัน

### 9.2 `retire_expired_accounts_sweep()` — ระงับบัญชีที่หมดอายุ

ทำงานทุก request เปลี่ยนสถานะบัญชีนักศึกษาที่สมัครมาครบ **365 วัน** ให้เป็น `retired` **บัญชีผู้ดูแลไม่ถูกระงับ**

### 9.3 การจับคู่เข้า-ออกด้วย `LEAD()`

ใช้ใน `/me/visits` แนวคิดคือให้แต่ละแถวมองเห็นแถวถัดไปของผู้ใช้คนเดียวกัน

- แถว `in` ที่แถวถัดไปเป็น `out` → การมาครั้งนั้น**จบแล้ว**
- แถว `in` ที่แถวถัดไปเป็น `in` หรือไม่มีแถวถัดไป → **ยังอยู่ในห้องสมุด**

วิธีนี้แม่นกว่าการ "หา out ตัวถัดไปที่ไหนก็ได้ข้างล่าง" ซึ่งจะจับคู่ผิดถ้าวันหนึ่งมีแถว `in` สองแถวติดกัน

### 9.4 การกัน cache ของไฟล์ CSS/JS

ฟังก์ชัน `ntc_asset_v()` เติม `?v=<เวลาที่ไฟล์ถูกแก้ล่าสุด>` ต่อท้าย URL ของไฟล์ asset ทุกไฟล์ พอไฟล์เปลี่ยน URL ก็เปลี่ยนตาม เบราว์เซอร์จึงโหลดของใหม่โดยไม่ต้องกดรีเฟรชแรง

---

## 10. ค่าคงที่และการตั้งค่า

### 10.1 ตัวแปรใน `.env`

| ตัวแปร | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `DB_HOST` | `localhost` | ที่อยู่ฐานข้อมูล |
| `DB_USER` | `root` | ผู้ใช้ฐานข้อมูล |
| `DB_PASSWORD` | (ว่าง) | รหัสฐานข้อมูล |
| `DB_NAME` | `library_checkin` | ชื่อฐานข้อมูล |
| `APP_ENV` | `dev` | `dev` หรือ `prod` — คุมความเข้มของ cookie |
| `APP_TIMEZONE` | `Asia/Bangkok` | เขตเวลา ต้องตรงกับนาฬิกาเครื่องที่ MySQL ใช้ |
| `LIBRARY_CLOSING_TIME` | `17:00` | เวลาปิดห้องสมุด ใช้คำนวณการปิดอัตโนมัติ |
| `LIBRARY_OPEN_DAYS` | `1,2,3,4,5` | วันที่เปิดทำการ เลขวันแบบ ISO-8601 (จันทร์ = 1 ... อาทิตย์ = 7) วันที่ไม่อยู่ในรายการนี้จะเช็คอินไม่ได้ |

**ไฟล์ `.env` ไม่อยู่ใน git** อยู่ใน `.gitignore` และไม่เคยถูก commit เลยแม้แต่ครั้งเดียว

ตัวอ่าน `.env` ตั้งค่าให้เฉพาะตัวแปรที่ยังไม่มีค่า ดังนั้น **ตัวแปรสภาพแวดล้อมจริงจะชนะค่าในไฟล์เสมอ** ซึ่งมีประโยชน์ตอนทดสอบ เช่น สั่งรันโดยตั้ง `LIBRARY_CLOSING_TIME=23:30` ชั่วคราวเพื่อทดสอบการเช็คอินนอกเวลาทำการ

### 10.2 คำนำหน้าชื่อที่ระบบรับ

`นาย` · `นาง` · `นางสาว`

### 10.3 แผนกวิชาทั้ง 14 แผนก

1. การจัดการสำนักงาน
2. การจัดการโลจีสติกส์และซับพลายเซน
3. การตลาด
4. การบัญชี
5. การโรงแรม
6. เทคโนโลยีธุรกิจดิจิทัล
7. ช่างกลโรงงาน
8. ช่างก่อสร้าง
9. ช่างยนต์
10. ช่างอิเล็กทรอนิกส์
11. ช่างเชื่อมโลหะ
12. ช่างไฟฟ้ากำลัง
13. ศิลปกรรม
14. เทคโนโลยีสารสนเทศ

รายการนี้เป็น **whitelist** ทั้งตอนสมัครสมาชิกและตอนเจ้าหน้าที่แก้ไขข้อมูล ทั้งสองทางใช้ฟังก์ชันตรวจสอบตัวเดียวกัน (`validate_student_profile()`) เพราะทั้งคู่เขียนลงคอลัมน์เดียวกัน — whitelist ที่บังคับแค่ทางเดียวคือ whitelist ที่มีทางเลี่ยง

### 10.4 ระดับชั้นและชั้นปี

| ระดับชั้น | ชั้นปีที่เลือกได้ |
|---|---|
| ปวช. | 1, 2, 3 |
| ปวส. | 1, 2 |

---

## 11. รายการฟังก์ชันทั้งหมด 231 ตัว

สร้างจากการสแกนซอร์สโค้ดโดยตรง เลขบรรทัดตรงกับไฟล์จริง

| ภาษา | จำนวน |
|---|---|
| PHP | 87 |
| JavaScript | 126 |
| **รวม** | **213** |


### 11.1 ฟังก์ชันฝั่งเซิร์ฟเวอร์ (PHP)


#### public/partials/admin-sidebar.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 12 | function admin_nav_class(string $href, string $active): string |  |

#### public/partials/head.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 9 | function ntc_asset_v(string $relPath): string | Cache-busting query string for /assets/* files: the deployed environment has swapped backends mid-session more than once (Docker <-> XAMPP) on the same http://l… |

#### public/partials/student-sidebar.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 12 | function student_nav_class(string $href, string $active): string |  |

#### src/auth.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 6 | function bootstrap_session(): void | Session cookie bootstrap |
| 32 | function require_login(): void | Mirrors app.py's @login_required. |
| 40 | function require_admin(): void | Mirrors app.py's @admin_required. |

#### src/constants.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 10 | function valid_prefixes(): array |  |
| 15 | function valid_departments(): array |  |
| 47 | function password_length_ok(string $password, ?string &$error = null): bool | True when the password is an acceptable length; $error receives the reason. |
| 72 | function valid_year_levels(string $level): array | Mirrors YEAR_OPTIONS in public/assets/js/constants.js. |
| 85 | function validate_student_profile(array $profile, ?string &$error = null): bool | Shared by signup (handle_register) and the admin edit form (handle_admin_member_update) |

#### src/db.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 4 | function get_db_connection(): PDO | PDO connection helper |

#### src/env.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 4 | function load_env(string $path): void | Minimal .env loader — no Composer dependency |
| 30 | function env(string $key, ?string $default = null): ?string |  |

#### src/handlers/admin_handlers.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 5 | function handle_admin_members(): void |  |
| 74 | function handle_admin_active_now(): void | Whoever's most recent checkin_logs row is 'in' is still inside |
| 104 | function handle_admin_force_checkout(): void | Admin manually ends someone's session — e.g |
| 133 | function handle_admin_reset_password(): void | Admin-initiated password reset |
| 158 | function handle_admin_reports(): void |  |
| 211 | function admin_target_user(PDO $conn, int $userId): array | Resolves the target of an admin action, or ends the request with the right error |
| 227 | function admin_count_other_admins(PDO $conn, int $excludingUserId): int | The system must never reach a state where nobody can administer it |
| 237 | function handle_admin_member_update(): void |  |
| 313 | function handle_admin_member_role(): void |  |
| 344 | function handle_admin_member_delete(): void |  |

#### src/handlers/announcement_handlers.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 16 | function ensure_settings_table(PDO $conn): void | สร้างตาราง app_settings แบบ IF NOT EXISTS ตอนใช้งานครั้งแรก เพราะโปรเจคนี้ไม่มีตัวรัน migration |
| 37 | function get_announcement(PDO $conn): array | อ่านประกาศปัจจุบัน คืนค่าเริ่มต้น (ว่าง/ปิด) เมื่อยังไม่เคยตั้ง |
| 68 | function handle_announcement(): void | GET /announcement — หน้าหลักนักศึกษาเรียกใช้ |
| 75 | function handle_admin_announcement_save(): void | POST /admin/announcement — บันทึกประกาศจากหน้าแอดมิน |

#### src/handlers/auth_handlers.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 5 | function handle_register(): void |  |
| 90 | function handle_login(): void |  |
| 138 | function handle_logout(): void |  |
| 162 | function handle_root_redirect(): void | Bare "/" has no page of its own — send browsers to the static login page |
| 172 | function retire_expired_accounts_sweep(): void | Suspends any student account whose users.created_at is a full academic year (365 days) in the past |

#### src/handlers/checkin_handlers.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 5 | function handle_checkin(): void | Ports app.py's /checkin |
| 70 | function handle_checkin_extend(): void | Ports app.py's /checkin/extend. |
| 112 | function handle_library_info(): void | Ports app.py's /library-info. |
| 145 | function auto_checkout_sweep(): void | Closes out anyone whose visit is over but who never pressed the button |

#### src/handlers/profile_handlers.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 4 | function handle_me(): void |  |
| 21 | function handle_change_password(): void |  |
| 71 | function my_visits_paired_sql(): string | One row per VISIT, not per event |
| 95 | function visits_date_param(string $key): ?string | Reads one YYYY-MM-DD query parameter, or null if it is absent or is not a real calendar date |
| 113 | function handle_my_visits(): void |  |
| 252 | function handle_my_history(): void |  |

#### src/helpers.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 3 | function json_response(mixed $data, int $status = 200): void | Sends a JSON response and stops execution |
| 11 | function json_error(string $message, int $status): void |  |
| 18 | function request_body(): array | Accepts JSON body or form-encoded body, same as Flask's `request.get_json(silent=True) or request.form`. |
| 31 | function body_str(array $body, string $key): string |  |
| 37 | function gender_from_prefix(?string $prefix): string | Mirrors app.py's gender_from_prefix(). |
| 49 | function client_ip(): string |  |
| 58 | function generate_temp_password(int $length = 8): string | Random temp password for admin-initiated resets (see handle_admin_reset_password in admin_handlers.php) |
| 71 | function to_isoformat(?string $mysqlDatetime): ?string | MySQL DATETIME comes back as "YYYY-MM-DD HH:MM:SS" — Python's .isoformat() on the same value produces "YYYY-MM-DDTHH:MM:SS" |
| 77 | function library_closing_time(): string | Mirrors app.py's LIBRARY_CLOSING_TIME env var (default 17:00). |
| 85 | function library_open_days(): array | วันที่ห้องสมุดเปิดทำการ เก็บเป็นเลขวันแบบ ISO-8601 (จันทร์ = 1 ... อาทิตย์ = 7) |
| 101 | function library_is_open_on(?DateTime $reference = null): bool | วันนี้ (หรือวันของ $reference) ห้องสมุดเปิดหรือไม่ |
| 108 | function library_open_days_label(): string | ชื่อวันภาษาไทยของวันที่เปิดทำการ ใช้ประกอบข้อความแจ้งผู้ใช้ |
| 122 | function library_closed_today_message(): string | ข้อความบอกว่าวันนี้ปิด ใช้ทั้งฝั่ง API และฝั่งหน้าเว็บ |
| 128 | function closing_datetime(DateTime $reference): DateTime | `$reference`'s date combined with library_closing_time(), as a DateTime. |
| 139 | function compute_planned_checkout(?int $durationMinutes, ?string $checkoutTime, ?DateTime $now = null): ?DateTime | Returns the planned checkout DateTime, clamped to today's closing time — or null, meaning "until closing" (no specific time chosen) |

#### src/rate_limit.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 25 | function check_login_rate_limit(PDO $conn, string $ip, string $username = ''): bool | True if this login attempt is allowed to proceed |
| 48 | function record_failed_login(PDO $conn, string $ip, string $username = ''): void |  |

#### src/reports/aggregate.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 14 | function build_filter_clause(array $filters): array | Builds WHERE-clause fragments (no leading AND) + matching params for the standard student/department filter set shared by every report |
| 39 | function distinct_student_values(PDO $conn, string $column): array | Distinct non-empty values for a students column, sorted |
| 49 | function aggregate_checkin_period(PDO $conn, string $startDate, string $endDate, array $filters = []): array |  |
| 95 | function pct_delta(int $current, int $previous): ?float | % change from previous -> current, or null if there's no previous-period baseline to compare against (avoids a division by zero reading as 0%). |
| 104 | function month_bounds(string $month): array | 'YYYY-MM' -> ['YYYY-MM-01', 'YYYY-MM-DD' (last day)]. |
| 114 | function thai_month_label(string $month): string | 'YYYY-MM' -> "เดือน<ชื่อเดือนไทย> <ปี พ.ศ.>", e.g |
| 125 | function previous_month(string $month): string |  |
| 136 | function ranked_breakdown(array $counts, int $limit = 8): array | dept_counts array (name => count) -> sorted list of {name, count} ranked by count desc, top $limit only. |
| 149 | function aggregate_hourly(PDO $conn, string $startDate, string $endDate, array $filters = []): array | Hour-of-day (0-23) event counts within [startDate, endDate] |
| 188 | function aggregate_weekly(PDO $conn, string $startDate, string $endDate, array $filters = []): array | Buckets events into "week N of the month" (ceil(day/7): days 1-7 = week 1, 8-14 = week 2, ...) rather than ISO week numbers |
| 220 | function aggregate_daily_trend(PDO $conn, string $startDate, string $endDate, array $filters = []): array | Per-day event counts across [startDate, endDate] inclusive, zero-filled for days with no data (so a trend chart never silently skips a day) |
| 263 | function aggregate_department_breakdown(PDO $conn, string $startDate, string $endDate, array $filters = []): array | Per-department breakdown — event count, unique students, % of total (always sums to ~100% since it's derived from the same $totalEvents), and rank |
| 305 | function aggregate_breakdown_by(PDO $conn, string $column, string $startDate, string $endDate, array $filters = []): array | Generalized version of aggregate_department_breakdown() for grouping by any single students column |
| 352 | function aggregate_monthly_totals(PDO $conn, array $filters = []): array | Per-calendar-month event counts with NO date bound |
| 376 | function build_summary_sentence(string $periodLabel, array $agg, ?array $busiestDay, array $topDepts, ?array $peakHour): string | Builds the auto-generated "สรุปสำหรับผู้บริหาร" paragraph (HTML) straight from server-computed aggregates |
| 412 | function render_month_select( | A month <select> listing recent months by name, used instead of <input type="month"> on every report filter |

#### src/reports/compare.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 3 | function handle_report_compare(): void | Ports app.py's admin_report_compare() + templates/report_compare.html. |

#### src/reports/daily.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 4 | function handle_report_daily(): void | Ports app.py's admin_report_daily() + templates/report_daily.html, extended with filters/summary/duration/sort per the report-system redesign. |

#### src/reports/dashboard.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 20 | function render_dashboard_sparkline(array $values, string $color, bool $isPdfExport = false): string | Tiny inline SVG trend line for a stat tile's sparkline |
| 46 | function render_dashboard_ring(float $pct, string $color): string | Ring gauge for the one genuine percentage in this report (a department's share of total traffic) |
| 70 | function aggregate_avg_session_minutes(PDO $conn, string $startDate, string $endDate, array $filters = []): ?float | Average minutes-per-visit within [startDate, endDate] |
| 104 | function format_minutes_thai(?float $minutes): string | "1 ชม |
| 122 | function aggregate_gender_breakdown(PDO $conn, string $startDate, string $endDate, array $filters = []): array | Male/female/unspecified split among unique visitors (not the whole roster) in [startDate, endDate] |
| 157 | function render_dashboard_delta(?float $delta, bool $compact = false): string | Signed delta badge (↑/↓ + %), colored by direction |
| 168 | function handle_report_dashboard(): void |  |

#### src/reports/department.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 5 | function handle_report_department(): void | Ports app.py's admin_report_department() + templates/report_department.html, extended with filters/ranking chart/MoM comparison/level cross-tab/ drill-down per … |

#### src/reports/executive.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 3 | function handle_report_executive(): void | Ports app.py's admin_report_executive() + templates/report_executive.html. |

#### src/reports/export.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 15 | function export_response(string $filenameBase, array $sections, string $format, array $meta = []): void | CSV/Excel export shared by every report except dashboard.php (dashboard is a print-only 1-pager, not a row-per-record report) |

#### src/reports/layout.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 11 | function render_pdf_bar_chart(array $labels, array $values, string $orientation = 'vertical', int $verticalHeight = 220, ?int $scaleMax = null): string | Renders one bar chart (vertical, e.g |
| 85 | function render_report_pdf(string $title, string $subtitle, string $content, string $extraStyle, string $filenameBase, array $pdfCharts = []): void | Renders $content (report-specific HTML built by the caller via ob_start()/ob_get_clean()) as a real PDF file via mPDF, instead of the browser's own print-to-PDF… |
| 281 | function render_report_layout(string $title, string $subtitle, string $content, string $extraStyle = '', array $exportUrls = [], array $pdfCharts = []): void | Ports templates/base_print.html |

#### src/reports/monthly.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 7 | function handle_report_monthly(): void | Ports app.py's admin_report_monthly() + templates/report_monthly.html, extended with filters/MoM+WoW comparisons/department ranking/executive summary per the re… |

#### src/reports/select.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 8 | function handle_report_select(): void | Ports templates/reports_select.html |

#### src/reports/semester.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 9 | function handle_report_semester(): void | New report (not in the original app.py) — the report-system redesign's §5 "รายงานภาคเรียน" |

#### src/reports/student_lookup.php

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 7 | function handle_report_student_lookup(): void | New report (not in the original app.py) — the report-system redesign's §6 "REPORT STUDENT/USER" |

### 11.2 ฟังก์ชันฝั่งเบราว์เซอร์ (JavaScript)


#### public/assets/js/admin-active.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 11 | function formatDurationThai(minutes) |  |
| 19 | function minutesSince(isoTimestamp) |  |
| 23 | function renderActive() |  |
| 92 | function tickDurations() | Recomputes the visible duration text every tick without re-fetching |
| 99 | async function loadActive() |  |
| 113 | async function forceCheckout(userId, name) |  |

#### public/assets/js/admin-announcement.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 8 | function annSetError(message) |  |
| 14 | function annUpdateCount() |  |
| 22 | function annShowMeta(data) |  |
| 35 | function annApply(data) |  |
| 42 | async function annSave(e) |  |

#### public/assets/js/admin-dashboard.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 13 | function isoWeekdayIndex(date) |  |
| 17 | function toISODate(d) |  |
| 28 | function formatThaiDate(dayStr) | Thai-locale label for a single day, e.g |
| 34 | function formatShortDate(dayStr) | Short axis tick, e.g |
| 39 | function filterRows(rows, view) |  |
| 56 | function dateRangeForView(view) | Every calendar day the current view covers, data or not |
| 79 | function computeStats(rows, view) |  |
| 155 | function describeTrendPoint(bar) |  |
| 164 | function niceCeiling(value) | Rounds the axis maximum up to a clean number so the ticks read 0 / 20 / 40 rather than 0 / 17 / 34 |
| 173 | function svgEl(name, attrs = {}) |  |
| 185 | function renderTrendChart(bars) | One bar per day |
| 305 | function departmentBreakdownForDay(day) | Every row (in + out) for one calendar day, grouped by department |
| 320 | function studentsInRowsForDept(rows, dept) | Individual students within one department, deduped from a set of checkin_logs rows |
| 340 | function renderDayDeptList(day) |  |
| 386 | function renderStudentList({ students, title, subtitle, onBack }) | Renders either a department's individual students, or (with no students yet resolved) is called by the two drill-down entry points below |
| 418 | function openDayModal(day) |  |
| 428 | function openDeptModal(dept) | Entry point from the "แผนกที่เข้าใช้มากที่สุด" panel |
| 441 | function closeDayModal() |  |
| 448 | function pad2(n) |  |
| 458 | function renderHourChart(stats) | Vertical bars: check-ins by hour of day, over the library's active hours only |
| 495 | function renderWeekdayChart(stats) | Horizontal bars: check-ins by weekday |
| 518 | function render() |  |
| 575 | function load() |  |

#### public/assets/js/admin-logs.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 8 | function computeLogsStats(rows) |  |
| 38 | function getFilteredRows() |  |
| 50 | function renderLogs() |  |
| 121 | async function loadLogs(params, { silent = false } = {}) |  |

#### public/assets/js/admin-members.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 9 | function populateMembersSelect(select, options, placeholder) |  |
| 36 | function setMembersRows(rows) | Every filter change re-queries, so the page always restarts at 1 there; only the pager itself moves it. |
| 42 | function memberBadges(row) |  |
| 55 | function renderMembersRows() |  |
| 122 | async function resetMemberPassword(userId, name) | No email/phone on file for students (see schema.sql), so a self-service "forgot password" email flow isn't possible |
| 142 | function showResetResultModal(name, tempPassword) | Big centered result popup for the temp password (replaces the old corner toast) |
| 149 | function hideResetResultModal() |  |
| 155 | function setMemberEditError(message) |  |
| 168 | function refreshEditYearOptions(keep) | ปวช |
| 177 | function openMemberEdit(row) |  |
| 218 | function closeMemberEdit() |  |
| 223 | async function saveMemberEdit() |  |
| 256 | async function toggleMemberRole() |  |
| 286 | async function deleteMember() |  |
| 313 | async function loadMembers() |  |

#### public/assets/js/api.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 6 | async function apiFetch(path, options = {}) |  |
| 44 | function escapeHtml(value) | Escapes a value for interpolation into an HTML template string |
| 54 | function apiGet(path) |  |
| 58 | function apiPostJson(path, body) |  |

#### public/assets/js/confirm-modal.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 15 | function showConfirmModal(message, { title = 'ยืนยันการทำรายการ', subject = '', confirmLabel = 'ยืนยัน', danger = false, icon } = {}) |  |
| 45 | function resolveConfirmModal(result) |  |

#### public/assets/js/dashboard.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 23 | function formatClock(totalSeconds) |  |
| 30 | function formatHM(date) |  |
| 34 | function closingDateFor(base, closing) |  |
| 41 | function toHHMM(date) |  |
| 59 | function timeToMinutes(hhmm) |  |
| 64 | function minutesToTime(total) |  |
| 68 | function fillWheel(el, values) |  |
| 79 | function wheelIndex(el) |  |
| 85 | function paintWheel(el) |  |
| 93 | function wheelValue(el) |  |
| 98 | function setWheelValue(el, value) |  |
| 112 | function clampWheels() | Pulls the selection back inside the open window once the scroll settles, so a time past closing can't be chosen at all |
| 126 | function scheduleWheelSettle(el) |  |
| 131 | function wireWheel(el) |  |
| 152 | function buildTimeWheels(bounds) |  |
| 173 | function readTimeWheels() |  |
| 179 | function timeInputBounds(closing) |  |
| 185 | function buildHourButtons(closing) |  |
| 194 | function getLast() |  |
| 198 | function isCheckedIn() |  |
| 203 | function isSameDay(a, b) |  |
| 210 | function checkedOutToday() | จบการใช้งานของวันนี้ไปแล้ว — เช็คอินแล้วและเช็คเอาต์แล้วในวันเดียวกัน |
| 217 | function getPlannedCheckoutAt() |  |
| 222 | async function loadHistory() |  |
| 228 | function isCheckinModalOpen() |  |
| 237 | function refreshHistoryQuietly() | The dashboard is the one page a student leaves open for hours, and its view of "am I checked in?" goes stale on its own: the auto-checkout sweep (backend-php/sr… |
| 252 | function startHistoryAutoRefresh() |  |
| 260 | function stopHistoryAutoRefresh() | Nothing navigates away from this page in-place today, but a timer that outlives the page it was started for is the kind of leak that only shows up once someone … |
| 265 | function render() |  |
| 336 | function restartElapsedTimer(checkedInSince) |  |
| 350 | function restartReminderWatcher(checkedInSince, plannedCheckoutAt) | Only relevant while this tab stays open |
| 372 | function showReminder(minutesLeft) |  |
| 377 | function hideReminder() |  |
| 381 | function stampDisabled() | ปุ่มถูกปิดได้จากสองเหตุผลที่ไม่เกี่ยวกัน (กำลังส่งคำขอ / วันนี้ห้องสมุดปิด) |
| 385 | function syncStampDisabled() |  |
| 389 | function setBusy(next) |  |
| 394 | async function performCheckin(body) |  |
| 409 | async function extendCheckout(minutes) |  |
| 421 | function handleStamp() |  |
| 433 | function setModalError(message) |  |
| 444 | function setModalTab(tab) |  |
| 460 | function renderHourButtons() |  |
| 481 | function renderHourButtonsSelection() |  |
| 493 | function openCheckinModal() |  |
| 511 | function closeCheckinModal() |  |
| 515 | function confirmModal() |  |
| 547 | function checkinUntilClosing() |  |
| 602 | function loadAnnouncement() | ประกาศจากเจ้าหน้าที่ — โหลดแยกจากประวัติ เพราะล้มเหลวคนละเรื่องกัน |

#### public/assets/js/header.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 6 | function initAppHeader() |  |

#### public/assets/js/history-modal.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 43 | function toDateInputValue(date) | Local date, not toISOString(): the college runs at UTC+7, and the ISO form is UTC, so any check-in before 07:00 would be filed under the previous day |
| 48 | function formatVisitTime(ts) |  |
| 54 | function formatDuration(minutes) | Minutes as something a person says out loud |
| 66 | function visitMinutes(visit) | null for a visit with no check-out yet |
| 81 | function presetRange(preset) | '' / '' means no filter at all, which is what the "ทั้งหมด" chip sends. |
| 100 | function syncRangeControls() | The date fields only exist for "กำหนดเอง" |
| 109 | function visitRowHtml(visit) | One <li> per visit: the day block, the in/out track, the duration. |
| 149 | function renderVisitList(visits) | Rows arrive newest-first and already sorted, so a run of the same month is always contiguous |
| 197 | function renderHistorySummary(summary) |  |
| 211 | function applyHistoryBounds(bounds) | Clamp the pickers to the days the student actually has rows in, so there is no way to select a month from before they enrolled and land on an empty list with no… |
| 224 | function renderHistoryPager(total, pageVisits) |  |
| 248 | function setHistoryBusy(busy) |  |
| 253 | async function loadHistoryPage() |  |
| 303 | function applyHistoryRange(preset, range) | Any change of range restarts at page 1 |
| 313 | function openHistoryModal() |  |
| 326 | function closeHistoryModal() |  |

#### public/assets/js/login.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 9 | function showLoginError(message) |  |
| 24 | function landingPathForRole(role) | Where each role's session belongs |

#### public/assets/js/pagination.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 17 | function paginateRows(rows, page, pageSize = PAGE_SIZE) | Clamps `page` into range and returns the slice plus everything the pager needs to describe itself |
| 27 | function pageWindow(page, totalPages) | First, last, and current ±1 — so a 1,400-page log doesn't render 1,400 buttons |
| 41 | function renderPager(container, state, onPage) | `onPage` receives the requested page number; the caller re-renders. |

#### public/assets/js/signup.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 3 | function populateSelect(select, options, placeholder) |  |
| 21 | function showSignupResultModal(success, title, message) | A message at the top of a long form is easy to miss once the reader has scrolled down filling in later fields |

#### public/assets/js/theme.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 10 | function computeDark(mode) | "auto" follows time of day (dark 18:00–06:00), matching the anti-flash script, not just system prefers-color-scheme. |
| 17 | function applyMode(mode) |  |
| 30 | function initThemeMenu() |  |

#### public/assets/js/toast.js

| บรรทัด | ฟังก์ชัน | หน้าที่ |
|---|---|---|
| 13 | function getToastContainer() |  |
| 24 | function showToast(message, { type = 'success', duration = 4000 } = {}) |  |

---

## 12. การทดสอบ

### 12.1 `tests/smoke_test.php`

ชุดทดสอบ end-to-end **26 เคส** เขียนด้วย PHP ล้วน ไม่ใช้ PHPUnit เพื่อให้สอดคล้องกับจุดยืนของโปรเจคที่ไม่พึ่ง framework

**วิธีรัน**
```
php tests/smoke_test.php http://localhost:8000
```

ต้องมี dev server และฐานข้อมูลจริงทำงานอยู่ (ทดสอบกับฐานข้อมูลจริง ไม่ใช้ mock) ชุดทดสอบสร้างบัญชีชั่วคราวของตัวเองและ**ลบทิ้งทุกครั้งเมื่อจบ**

**ครอบคลุม** สมัครสมาชิก → ล็อกอิน → เช็คอิน/เช็คเอาต์ → ดูประวัติ → เปลี่ยนรหัสผ่าน → ล็อกอินด้วยรหัสเก่าต้องไม่ผ่าน → สิทธิ์ผู้ดูแล → เปิดรายงานทุกแบบ

### 12.2 ข้อควรระวังที่สำคัญมาก

**ชุดทดสอบผูกกับเวลาจริง** มี 5 เคสที่ต้องเช็คอิน ถ้ารันหลัง `LIBRARY_CLOSING_TIME` (ค่าเริ่มต้น 17:00) ทั้ง 5 เคสจะไม่ผ่านพร้อมข้อความ "ห้องสมุดปิดแล้ว ไม่สามารถเช็คอินได้"

**นี่คือพฤติกรรมที่ถูกต้องของระบบ ไม่ใช่บั๊ก** แต่ทำให้ตีความผลพลาดได้ถ้าไม่รู้มาก่อน วิธีรันนอกเวลาทำการคือตั้งตัวแปรสภาพแวดล้อมชั่วคราวตอนสตาร์ต server

**ชุดทดสอบผูกกับวันในสัปดาห์ด้วย** ห้องสมุดเปิดจันทร์-ศุกร์ ถ้ารันวันเสาร์หรืออาทิตย์ เคสที่ต้องเช็คอินจะไม่ผ่านพร้อมข้อความ "วันนี้ห้องสมุดปิดทำการ" ซึ่งก็เป็นพฤติกรรมที่ถูกต้องเช่นกัน

สั่งรันนอกเวลาทำการและนอกวันทำการได้ด้วยการตั้งตัวแปรสภาพแวดล้อมทั้งสองตัว

```
LIBRARY_CLOSING_TIME=23:30 LIBRARY_OPEN_DAYS=1,2,3,4,5,6,7 php -S localhost:8000 -t public
```

### 12.3 การตรวจ syntax

```
php -l <ไฟล์>       # ตรวจไฟล์ PHP
node --check <ไฟล์>  # ตรวจไฟล์ JavaScript
```

ขั้นตอน deploy รัน `php -l` กับไฟล์ PHP **ทุกไฟล์** ก่อนอัปโหลด เพราะไฟล์เดียวที่ syntax ผิดทำให้ทั้งเว็บขาวได้ และการกู้คืนต้องอัปทับใหม่ผ่าน FTP ซึ่งช้ากว่าการหยุดตั้งแต่ตอนตรวจมาก

---

## 13. การนำขึ้นเซิร์ฟเวอร์

### 13.1 โครงสร้างบนเซิร์ฟเวอร์จริง (Hostinger)

```
/                          ← chroot ของบัญชี FTP
    .env                   ← ห้ามแตะ ไม่มีใน git
    vendor/                ← mPDF ห้ามแตะ ไม่มีใน git
    fonts/                 ← ฟอนต์ .ttf สำหรับ mPDF
    src/                   ← มาจาก backend-php/src/
    public_html/           ← document root มาจาก backend-php/public/
```

### 13.2 ขั้นตอน deploy

ใช้ GitHub Actions (`.github/workflows/deploy.yml`) สั่งรันด้วยมือจากแท็บ Actions

**ช่องที่ต้องกรอก**

| ช่อง | ความหมาย |
|---|---|
| `dry_run` | ซ้อมก่อน ไม่เขียนไฟล์จริง ควรรันรอบนี้ก่อนเสมอ |
| `confirm` | ต้องพิมพ์คำว่า `deploy` เพื่อยืนยัน |
| `since_ref` | อัปไฟล์ที่เปลี่ยนตั้งแต่ commit ไหน |

**Secret ที่ต้องตั้งไว้ใน GitHub** — `FTP_SERVER`, `FTP_USERNAME`, `FTP_PASSWORD`

### 13.3 ลำดับการอัปไฟล์ (สำคัญ)

ไฟล์ถูกอัปตามลำดับที่กำหนดไว้ ไม่ใช่ sync ทั้งโฟลเดอร์พร้อมกัน เพราะไฟล์ใน `src/` กับ `public/` พึ่งพากัน ถ้าอัปสลับลำดับจะมีช่วงที่เว็บพังกลางคัน

1. `src/constants.php`
2. `public/partials/guard.php`
3. `src/auth.php`
4. ไฟล์อื่นๆ ใน `src/` ที่เปลี่ยน
5. ไฟล์อื่นๆ ใน `public/` ที่เปลี่ยน
6. `public/index.php` **ท้ายสุดเสมอ** เพราะเป็นตัวประกาศ route

### 13.4 การตรวจสอบหลังอัป

workflow เรียกเว็บจริงเพื่อตรวจ

```
/               ต้องได้ 302 (เด้งไปหน้าล็อกอิน)
/login          ต้องได้ 200
/library-info   ต้องได้ 401 (ยังไม่ล็อกอิน)
/src/auth.php   ต้องได้ 404 ← สำคัญที่สุด
```

บรรทัดสุดท้ายคือด่านความปลอดภัย ถ้า `/src/auth.php` ตอบอย่างอื่นที่ไม่ใช่ 404 แปลว่าอัปไฟล์ผิดชั้นและโค้ดฝั่งเซิร์ฟเวอร์กำลังเปิดให้คนนอกอ่านได้ workflow จะ fail ทันที

### 13.5 ข้อควรระวังด้านความปลอดภัย

**FTP ส่งข้อมูลแบบไม่เข้ารหัส** ทั้งชื่อผู้ใช้ รหัสผ่าน และเนื้อไฟล์เดินทางเป็นข้อความเปล่า ควรเปลี่ยนไปใช้ FTPS (เติม `--ssl-reqd` ในคำสั่ง curl) เมื่อมีโอกาส

**ชื่อบัญชี FTP และชื่อโฮสต์อยู่ในคอมเมนต์ของ repo ซึ่งเป็น public** ใครก็อ่านได้ ความปลอดภัยทั้งหมดจึงแขวนอยู่กับรหัสผ่านตัวเดียว ควรตั้งให้ยาวและสุ่ม

---

## ภาคผนวก — สรุปตัวเลขของระบบ

| หัวข้อ | จำนวน |
|---|---|
| ตารางในฐานข้อมูล | 5 |
| หน้าเว็บ | 9 |
| API endpoint | 28 |
| รายงาน | 8 แบบ |
| ฟังก์ชัน PHP | 87 |
| ฟังก์ชัน JavaScript | 126 |
| **ฟังก์ชันรวม** | **213** |
| ไฟล์ที่มีฟังก์ชัน | 39 |
| แผนกวิชา | 14 |
| เคสทดสอบ | 26 |

---

*เอกสารนี้สร้างจากการอ่านซอร์สโค้ดจริงทีละไฟล์ ณ commit `fd4f2c4`*

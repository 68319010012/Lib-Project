# Leave empty for local dev — the Vite proxy (vite.config.js) keeps API calls
# same-origin. Set to the real PHP backend's origin (e.g. https://api.example.com)
# only for production builds.
VITE_API_BASE_URL=
